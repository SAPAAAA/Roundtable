#header-left-sidebar-toggle {
    display: none !important;
}

.icon-button {
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.3);
    border-radius: 50%;
    background-color: rgba(255, 255, 255, 0.1); /* Optional contrast */
}

@media (max-width: 768px) {
    #header-left-sidebar-toggle {
        display: inline-block !important;
    }
}
